# Offline portal image: the Angular app is built outside (ng build) and copied in.
# nginx.conf is mounted at run time (infra/nginx/nginx.minimal.conf). Context = kit root.
FROM nginx:alpine
COPY portal-dist/ /usr/share/nginx/html
EXPOSE 80 443
