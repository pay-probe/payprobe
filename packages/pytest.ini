[pytest]
# Repo-wide config so `cd packages && pytest worker/tests orchestrator/tests`
# runs both packages with one async mode. Per-package pyproject configs still
# apply when a package is tested in isolation.
asyncio_mode = auto
markers =
    flaky: known-flaky test, quarantined with a written reason (see README).
