"""PayProbe insight service (ADR-0005).

Read-only advisory service over run history: failure categorization,
instance-specific failure explanation, and run-outcome / flakiness
prediction. The model advises; the registry and the gates decide.
"""
